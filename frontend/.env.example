# Point the dashboard at the deployed scorer. Without this it falls back to
# http://localhost:8000, which is correct for local development and wrong the
# moment the page is served from anywhere else.
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app
